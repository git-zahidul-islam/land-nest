const SectionTitle = ({heading,extra}) => {
    return (
        <section>
            <h1 className="md:text-4xl text-xl font-bold">{heading}</h1>
        </section>
    );
};

export default SectionTitle;